version: "2"

services:
  consul:
    restart: always
    command: -server -client 0.0.0.0 -join ${CONSUL_MASTER} --data-dir /var/consul -ui
    image: gliderlabs/consul-agent:0.6
    ports:
      - 8400:8400
      - 8500:8500
      - 8600:53/udp
      - 53:53/udp